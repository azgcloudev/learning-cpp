#include "../../headers/std_lib_facilities.h"

int main()                      // int = is the type; main is the function name; () = contains the parameters
{
    // function body
    cout << "Hello World!\n";   // the line is a statement
    keep_window_open();         // wait for a character to be enter
    return 0;
}